# actividad3-daw

Página HTML basada en la noticia "Gran Fin de Semana de Pascuas en Funes".

Incluye:
- HTML semántico
- Navegación con `<nav>`
- Formulario con `<form>`, `<input>`, `<label>` y `<button>`